<div class="p-6 md:p-8"><div class="mb-6"><h1 class="text-2xl font-bold text-slate-900">Role & Permission</h1><p class="mt-1 text-sm text-slate-500">Atur akses Editor, Publisher, Admin, Viewer, dan role lainnya. Super Admin selalu memiliki semua akses.</p></div>@if(session('role-message'))<div class="mb-5 rounded-xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-700">{{ session('role-message') }}</div>@endif<div class="space-y-5">@foreach($roles as $role)<section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div class="flex flex-wrap items-center justify-between gap-3"><div><h2 class="text-lg font-bold">{{ $role->name }}</h2><p class="text-xs text-slate-500">{{ $role->users()->count() }} pengguna</p></div>@if($role->name!=='Super Admin')<button wire:click="save({{ $role->id }})" class="rounded-xl bg-blue-600 px-4 py-2 text-sm font-semibold text-white">Simpan Permission</button>@else<span class="rounded-full bg-emerald-100 px-3 py-1 text-xs font-bold text-emerald-700">Full Access</span>@endif</div><div class="mt-5 grid gap-3 md:grid-cols-2 xl:grid-cols-3">@foreach($permissionOptions as $key=>$label)<label class="flex items-center gap-3 rounded-xl border border-slate-200 p-3 text-sm {{ $role->name==='Super Admin'?'bg-slate-50':'' }}"><input type="checkbox" value="{{ $key }}" wire:model="permissionsByRole.{{ $role->id }}" @disabled($role->name==='Super Admin') @checked($role->name==='Super Admin')><span><strong>{{ $label }}</strong><small class="block text-slate-400">{{ $key }}</small></span></label>@endforeach</div></section>@endforeach</div></div>
