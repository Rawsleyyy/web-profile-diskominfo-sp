<div class="p-6 md:p-8"><div class="mb-6"><h1 class="text-2xl font-bold text-slate-900">Preset Instansi</h1><p class="mt-1 text-sm text-slate-500">Mulai konfigurasi dengan pola umum instansi. Preset mengatur modul, navbar, section beranda, dan theme draft; konten berita/artikel/dokumen tidak dihapus.</p></div>@if(session('preset-message'))<div class="mb-5 rounded-xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-700">{{ session('preset-message') }}</div>@endif<div class="grid gap-5 md:grid-cols-2 xl:grid-cols-3">@foreach($presets as $key=>$preset)<article class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div class="text-lg font-bold">{{ $preset['label'] }}</div><p class="mt-2 min-h-10 text-sm text-slate-500">{{ $preset['description'] }}</p><div class="mt-4 flex flex-wrap gap-1">@foreach($preset['modules'] as $module)<span class="rounded-full bg-slate-100 px-2 py-1 text-[11px]">{{ $module }}</span>@endforeach</div><button wire:click="apply('{{ $key }}')" wire:confirm="Terapkan preset ini ke draft? Struktur navbar draft akan disusun ulang." class="mt-5 rounded-xl bg-blue-600 px-4 py-2.5 text-sm font-semibold text-white">Gunakan Preset</button></article>@endforeach</div></div>
